gem 'sinatra', '~> 0.9.4'
gem 'warden',  '~> 0.5.0'

gem 'rake',                         :only => [:test]
gem 'rspec',            '~> 1.2.9', :only => [:test], :require_as => 'spec'
gem 'yard',                         :only => [:test]
gem 'rack-test',        '~> 0.5.0', :only => [:test], :require_as => 'rack/test'
gem 'rcov',                         :only => [:test]

disable_system_gems